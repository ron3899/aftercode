# Dev only. Copy to `.env.development` (loaded by `vite dev`, NOT by `vite build`)
# so the backend URL never gets baked into the production bundle. In production
# the SPA is served by the backend and uses the same origin (empty base).
VITE_API_BASE=http://localhost:8080
